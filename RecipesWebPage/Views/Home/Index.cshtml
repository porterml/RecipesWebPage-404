@{
    ViewData["Title"] = "Home Page";
}

@model List<Recipe>


<h1>Recipes</h1>

<ul>
    @foreach (var r in Model)
            {
                <li>@r.RecipeID - @r.RecipeTitle</li>
            }


</ul>
